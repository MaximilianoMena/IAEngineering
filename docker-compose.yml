services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./chroma_db:/app/chroma_db
      - ./observabilidad:/app/observabilidad
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000
